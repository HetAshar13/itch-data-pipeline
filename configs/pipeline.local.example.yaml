project:
  name: itch-data-pipeline
  environment: local

paths:
  input_root: data/input
  output_root: outputs/local
  log_root: logs/local

processing:
  schema_version: "1.0"
  overwrite_partitions: true
  default_date: "2019-05-30"
  default_symbol: "AAPL"

meatpy:
  enabled: true
  sample_mode: true
